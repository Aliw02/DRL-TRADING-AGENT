# config/config_sac.yaml
# FINAL, HIGH-THROUGHPUT CONFIGURATION

training:
  learning_rate: 0.0002
  buffer_size: 75000
  batch_size: 128
  tau: 0.005
  gamma: 0.99
  
  # Continuous (Online) Training Strategy
  train_freq: [1, "step"]
  gradient_steps: 1
  learning_starts: 10000
  
  walk_forward:
    n_splits: 5
    min_train_size: 40000
    test_size: 30000
    timesteps_per_split: 100000
    eval_freq: 10000

  fine_tuning:
    recent_data_years: 2.5
    total_timesteps: 100000
    learning_rate: 0.00002

model:
  features_dim: 256
  # CNN channels can be slightly increased as the model is simpler
  cnn_out_channels: 64

specialist_training:
  min_samples_per_regime: 5000

environment:
  initial_balance: 10000
  sequence_length: 70
  commission_pct: 0.0005
  max_account_drawdown: 0.20

  
  
  daily_growth_reward_factor: 40.0
  volatility_penalty_factor: 0.7

  
  monthly_profit_target_pct: 0.13  # 50% target
  monthly_bonus_reward: 130.0     
  steps_per_month: 2016